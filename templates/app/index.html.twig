{% extends "base.html.twig" %}

{% block body %}
    <h1 class="h3 mt-5">Liste des fermes</h1>
    <hr>

    <div class="row">
        {% for farm in farms %}
            <div class="col-md-4">
                <div class="card">
                    <div class="card-hearder">
                        {{ farm.name }}
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock body %}